# 技術報告：EES 同步與批次處理優化

## 1. 執行摘要 (Executive Summary)
針對 `go-upf` EES 模組在連續處理多個訂閱請求時出現的「不良協同效應 (Bad Synergy)」問題，我們進行了診斷與修復。主要症狀包括流量測量重複、啟動時間錯開以及高磁碟 I/O。我們實作了**共享串流架構 (Shared Streaming Architecture)** 與**網路時鐘錨定 (Network-Clock Anchoring)**，以確保多個 UE IP（例如 `10.10.0.1`–`.3`）能以完美同步的批次進行處理，且不會增加記憶體開銷。

---

## 2. 問題診斷：為何產生「不良協同效應」？
當連續建立三個訂閱時，系統會觸發三個獨立的工作流，導致以下三個失效點：

*   **時間錨定抖動 (Anchor Jitter)：** 系統原先使用 `time.Now()`（封包抵達時間）來錨定歷史數據。由於 OS 排程和網路延遲的波動，每個 IP 的歷史時間軸會產生數毫秒至數秒的偏移，導致與即時數據銜接時出現間隙或重疊。
*   **I/O 競爭與錯位啟動：** 每個訂閱都會啟動一次完整的 Parquet 檔案掃描。對於大檔案，這會導致磁碟 I/O 損耗。各個 IP 完成「預熱 (Warm-up)」的時間點不同，導致無法同時開始即時報告。
*   **測量數據加倍 (Double Counting)：** 聚合器 (Aggregator) 的合併邏輯是累加性的。如果偽驅動程式 (Pseudo-driver) 和內核 (Kernel) 同時回報同一個時間視窗的數據（由於時間錨定抖動），系統會將其相加，導致回報 200% 的實際流量。

---

## 3. 解決方案實作

### A. 共享 Parquet 讀取器（訂閱批次化）
我們將「獨立讀取器」模型改為**合併掃描 (Coalescing Pass)** 模型。
*   **機制：** 當第一個訂閱抵達時，`PseudoDriver` 會進入一個 **500ms 的等待視窗**。在此視窗內抵達的所有訂閱都會被歸類到同一個批次。
*   **單次掃描串流：** 驅動程式對 Parquet 檔案僅執行**一次**讀取。每讀取一行，就會針對整個批次進行檢查，並將數據分發到對應的 IP 緩衝區。
*   **OOM 安全性：** 系統維持逐行串流處理，確保記憶體使用量不會隨檔案大小增加，僅隨活躍訂閱數量微幅增加。

### B. 網路時鐘錨定 (Network-Clock Anchoring)
我們將系統從「牆上時鐘 (Wall Clock)」（抵達時間）切換為「網路時鐘 (Network Clock)」（協定時間）。
*   **修復：** `PseudoDriver` 現在使用內核 URR（使用量報告規則）提供的 **`EndTime`** 欄位來錨定交接點。
*   **結果：** 歷史數據與即時數據現在會在一個確定的時間戳準確銜接，消除了處理延遲導致的間隙與重疊。

### C. 基於來源優先級的合併邏輯
我們重構了 `Aggregator`，使其能智慧處理數據衝突。
*   **邏輯：** 在 `consolidateWithPriority` 中，系統現在會檢查數據的來源 (`Source`)。
*   **規則：** **內核（即時）數據永遠優先於偽驅動（模擬）數據。** 如果同一個視窗同時存在兩者，則捨棄模擬數據，採用網路真實數據，從而防止數據加倍。

---

## 4. 優化後的工作流程（以 3 個 UE IP 為例）
針對 `10.10.0.1`、`10.10.0.2` 與 `10.10.0.3` 的訂閱：

1.  **API 層：** 接收到三個 POST 請求，各自呼叫 `ScheduleReplay`。
2.  **PseudoDriver：** 在 500ms 內收集這三個 IP。啟動**一次**磁碟讀取程序。
3.  **內核訊號：** 第一個即時 URR 抵達，提供精確的時間戳（例如 `12:00:05`）。
4.  **對齊：** 所有三個 IP 立即同時錨定在 `12:00:05`。
5.  **交接：** `PseudoDriver` 完成歷史回放。`Aggregator` 在**同一個計時週期**內切換到這三個 IP 的即時報告。
6.  **通知：** 訂閱者收到一份乾淨、無重複數據且包含所有三個 IP 的 REST 通知。

---

## 5. 剩餘風險與建議
*   **異質報告週期：** 目前的批次處理假設同一個批次內的訂閱共用相同的 `PeriodSec`。如果不同 IP 需要不同的週期，建議發送請求時應間隔 500ms 以上以觸發獨立掃描。
*   **錨定精度：** 系統目前對 `GridAnchor` 進行秒級取整。若需要極高頻率（<1s）的報告，可能需要進一步調整至亞秒級精度。

---
**狀態：** 所有修復已合併並通過編譯。系統現已針對批次 UE 處理進行優化，並能安全處理大檔案 (OOM-safe)。
